@model List<ResultOurServiceDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Layout/Index.cshtml";
    int count = 0;
}

<h6 class="mb-4">UI Hizmetlerimiz Alanı</h6>
<table class="table table-hover">
    <thead>
        <tr class="bg-light">
            <th>#</th>
            <th>Başlık</th>
            <th>Açıklama</th>
            <th>Resim</th>
            <th>Yayında mı?</th>
            <th>İşlemler</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            count++;
            <tr>
                <td>@count</td>
                <td>@item.Title</td>
                <td>@item.Description</td>
                <td>@item.ImageUrl</td>
                <td>@item.Status</td>
                <td>
                    <a href="/Admin/OurService/UpdateOurService/@item.OurServiceId" class="btn btn-secondary">Detay</a>
                    <a href="/Admin/OurService/DeleteOurService/@item.OurServiceId" class="btn btn-danger">Sil</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<a href="/Admin/OurService/CreateOurService" class="btn btn-primary">+ Yeni Oluştur</a>

