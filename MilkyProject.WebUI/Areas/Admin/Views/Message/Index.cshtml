@model List<ResultMessageDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Layout/Index.cshtml";
    int count = 0;
}

<h6 class="mb-4">UI Gelen Mesaj Alanı</h6>
<table class="table table-hover">
    <thead>
        <tr class="bg-light">
            <th>#</th>
            <th>Gönderen Adı</th>
            <th>Konu</th>
            <th>Email</th>
            <th>İşlemler</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            count++;
            <tr>
                <td>@count</td>
                <td>@item.Name</td>
                <td>@item.Subject</td>
                <td>@item.Email</td>
                <td>
                    <a href="/Admin/Message/MessageDetail/@item.MessageId" class="btn btn-secondary">Detay</a>
                    <a href="/Admin/Message/DeleteMessage/@item.MessageId" class="btn btn-danger">Sil</a>
                </td>
            </tr>
        }
    </tbody>
</table>